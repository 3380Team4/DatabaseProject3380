@model IEnumerable<ThemeParkApplication.Models.Maintenance>
@{
    ViewData["Title"] = "generateReportAsync";
}
<br /><br />
<h2>Report</h2>

<table class="table">
    <thead>
        <tr>
            <th>
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.WorkStartDate)
                </td>
            </tr>
        }
    </tbody>
</table>

<div>
    <a asp-action="Index">Back to List</a>
</div>